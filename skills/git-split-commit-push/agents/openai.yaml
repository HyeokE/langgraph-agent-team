interface:
  display_name: "Split Commit Push"
  short_description: "변경사항을 논리 커밋으로 분할하고 안전하게 push"
  default_prompt: "Use $git-split-commit-push to split current changes into logical commits and push them safely."
